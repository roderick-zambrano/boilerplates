apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: speedtest-tracker-data
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: "pv-speedtest-tracker-data"
status: {}
